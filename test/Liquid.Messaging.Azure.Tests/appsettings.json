{
  "liquid": {
    "messaging": {
      "azure": {
        "TestAzureServiceBus": {
          "connectionString": "${{ secrets.LIQUID_MESSAGING_AZURE_TEST_CONNECTIONS }}"
        }
      }
    }
  }
}
